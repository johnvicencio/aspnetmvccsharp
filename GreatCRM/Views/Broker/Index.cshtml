@model GreatCRM.Models.ViewModels.RecordViewModels

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Broker.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Broker.Age)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model.Brokers) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Age)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BrokerId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BrokerId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BrokerId })
        </td>
    </tr>
}

</table>
